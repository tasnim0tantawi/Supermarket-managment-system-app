import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'itcs444-project',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
